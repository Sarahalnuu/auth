// import express from 'express'
// import validate from '../middleware/validate';
// import getAllteacher, { addteacher } from '../controller/teacher.controller'

// const router = express.Router();
//  router.get('/', getAllteacher);
//  router.get('/teacher/:teacherid', getidteacher);
//  router.post('/', validate(addteachertSchema), addteacher);

// export default router;